#include <stdio.h>

// Estructura para representar un documento
struct Documento {
    int id; // Identificador único del documento
    char titulo[100]; // Título del documento
    char contenido[1000]; // Contenido del documento
};